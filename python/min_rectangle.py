
def solution(sizes):
    answer = 0

    w = sorted(sizes, key=lambda x: x[0], reverse=True)
    h = sorted(sizes, key=lambda x: x[1], reverse=True)

    if(w[0][0])

    print(w)
    print(h)
    return answer


print(solution([[60, 50], [30, 70], [60, 30], [80, 40]]))
