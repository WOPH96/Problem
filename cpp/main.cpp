#include <string>
#include <vector>
#include <iostream>

using namespace std;

vector<vector<int>> solution(vector<vector<int>> arr1, vector<vector<int>> arr2)
{
    vector<vector<int>> answer;

    auto a = arr1.size();
    auto b = arr1[0].size();
    auto c = arr1[1].size();

    cout << a << b << c;

    return answer;
}

int main()
{
    return 0;
}