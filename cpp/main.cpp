#include <string>
#include <vector>
#include <iostream>

using namespace std;

// 소수 = 이전의 소수로 안나눠떨어지면 소수?

int solution(int n)
{
    vector<int> prime;
    prime.push_back(2);

    for (int i = 2; i <= n; i++)
    {
        while ()
    }

    return prime.size();
}

int main()
{

    cout << solution(10) << endl;

    return 0;
}