#include <vector>
#include <iostream>

using namespace std;
int main()
{
    std::ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    freopen("cote_input.txt", "r", stdin);

    // 자신보다 같거나 높은 빌딩 개수 구하기
    // return 은 모든 빌딩에서 구한 값의 합
    // 자신 기준 가장 높은 값만 구하면 될듯?
    // heigths -> 30만개 최대

    return 0;
}
