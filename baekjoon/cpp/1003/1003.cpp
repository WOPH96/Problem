#include <bits/stdc++.h>
using namespace std;

// vector<string> split(string &str, const char &dim = ' ')
// {
//     vector<string> vs;
//     size_t previous = 0, current;
//     string temp;
//     current = str.find(dim);

//     while (current != string::npos)
//     {
//         temp = str.substr(previous, current - previous);
//         vs.push_back(temp);
//         previous = current + 1;
//         current = str.find(dim, previous);
//     }
//     vs.push_back(str.substr(previous));
//     return vs;
// }

vector<pair<long, long>> fibo(n)
{
    if (n == 0)
        return
}

int main()
{
    std::ios_base::sync_with_stdio(false);
    freopen("1003_input.txt", "r", stdin);

    int t;

    cin >> t;
    vector<int> n(t);
    for (int i = 0; i < t; i++)
    {
        cin >> n[i];

        //
    }

    // string abc = "my name is wonphil";
    // vector<string> vs = split(abc);
    // for (auto &elem : abc)
    // {
    //     cout << elem << endl;
    // }

    return 0;
}
